import { StyleSheet } from 'react-native';

export const mainStyles = StyleSheet.create({
    container : {
        alignItems : "center",
        flex: 1
    },
    //main styles
    content : {
    }
})